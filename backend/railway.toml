# Railway Deployment Configuration
# This tells Railway how to build and start the backend

[build]
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
